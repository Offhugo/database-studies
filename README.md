# 📊 SQL Analytics: Fundamentos e Extração de Métricas

Este repositório reúne meus estudos práticos em **SQL**, focados na extração de informações estratégicas e geração de relatórios através de consultas estruturadas e funções de agregação. 

Diferente de apenas armazenar exercícios, este projeto simula cenários reais onde a manipulação de dados é essencial para a tomada de decisão.

## Competências Demonstradas

### 🔹 Extração e Filtragem (DQL)
Capacidade de navegar em grandes conjuntos de dados utilizando filtros precisos:
- Uso de `WHERE` com operadores lógicos (`AND`, `OR`, `NOT`) e de comparação.
- Filtragem de padrões de texto com `LIKE` e `IN`.
- Ordenação estratégica de resultados com `ORDER BY`.

### 🔹 Inteligência com Agregações
Transformação de dados brutos em métricas de negócio:
- **Contagem Operacional:** Uso de `COUNT()` para quantificar registros (ex: total de vendas, volume de alunos).
- **Cálculos Financeiros/Estatísticos:** Aplicação de `SUM()`, `AVG()`, `MIN()` e `MAX()` para obter totais e médias.
- **Segmentação de Dados:** Domínio de `GROUP BY` para categorizar métricas por grupos.
- **Filtros de Grupo:** Uso de `HAVING` para validar condições em dados já agregados.
